# 1. Crear la carpeta del proyecto
mkdir altum
cd altum

# 2. Crear el entorno virtual (llamado "venv")
python -m venv venv

# 3. Activar el entorno (verás que aparece (venv) en tu terminal)
source venv/Scripts/activate